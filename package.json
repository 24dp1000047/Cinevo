{
  "name": "cinevo",
  "version": "1.0.0",
  "private": true,
  "description": "Cinevo - Standalone Streaming Platform",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "build:pages": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm run start --prefix frontend",
    "install:all": "npm --prefix frontend install"
  },
  "devDependencies": {
    "wrangler": "^4.128.0"
  }
}
