{
  "name": "cinevo",
  "version": "1.0.0",
  "private": true,
  "description": "Cinevo - Public-First Streaming Platform",
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"bgBlue.bold,bgMagenta.bold\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "cd frontend && npm install && npx opennextjs-cloudflare build",
    "build:all": "npm run build --prefix backend && npm run build --prefix frontend",
    "deploy": "wrangler deploy",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "wrangler": "^4.128.0"
  }
}
